{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "DEBUG MAIN",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/main.go",
      "cwd": "${workspaceFolder}",
      "buildFlags": "-race -v"
    }
  ]
}